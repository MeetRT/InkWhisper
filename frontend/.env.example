# Copy this to .env and adjust values for your environment.
# The frontend reads this at build time (Vite env vars must be prefixed VITE_).

# Base URL of the InkWhisper backend API (no trailing slash)
VITE_API_BASE_URL=http://localhost:3001
